---
import Base from "../layouts/Base.astro";
import ProjectItem from "../components/ProjectItem.astro";
import { site } from "../site.config";
import { resume } from "../resume.config";
import { sortedProjects } from "../lib/projects";

const projects = await sortedProjects();
const featured = projects.filter((p) => p.data.featured);
const other = projects.filter((p) => !p.data.featured);
---

<Base title={`${site.name} — Robotics & Perception Engineer`} description={`${site.positioning} ${site.seeking}`} full>
  <div class="portfolio-home">
    <section class="portfolio-hero" id="about" aria-labelledby="hero-title">
      <p class="eyebrow">Jose Sanchez Gonzalez · M.S. Artificial Intelligence</p>
      <h1 id="hero-title">{site.positioning}</h1>
      <p class="hero-role">{site.seeking}</p>
      <p class="hero-intro">Watch a pruning arm approach a branch, check its sensors, and return home. Explore the depth models, humanoid evaluations, and retrieval software in four visual case studies.</p>
      <ul class="link-row hero-actions">
        <li><a class="button-link" href="#projects">Explore selected work</a></li>
        <li><a href={resume.links.pdf}>Download résumé <span class="file-type">PDF</span></a></li>
        <li><a href={`mailto:${site.email}`}>Get in touch</a></li>
      </ul>
    </section>

    <section class="work-section" id="projects" aria-labelledby="work-title">
      <div class="section-heading">
        <div>
          <p class="eyebrow">Selected work</p>
          <h2 id="work-title">Robotics first. Evidence throughout.</h2>
        </div>
        <p>Start with a demo. Follow the case study to the code, evaluation, and tradeoffs.</p>
      </div>
      <ul class="project-grid">
        {featured.map((project) => <ProjectItem project={project} number={projects.indexOf(project) + 1} />)}
      </ul>
    </section>

    <section class="work-section" aria-labelledby="more-title">
      <div class="section-heading">
        <div>
          <p class="eyebrow">Further work</p>
          <h2 id="more-title">Manipulation, language, and 3D learning</h2>
        </div>
        <p>Focused experiments and coursework, with their current limits stated alongside the results.</p>
      </div>
      <ul class="project-grid">
        {other.map((project) => <ProjectItem project={project} number={projects.indexOf(project) + 1} />)}
      </ul>
    </section>

    <section class="about-work" id="resume" aria-labelledby="background-title">
      <div>
        <p class="eyebrow">Background</p>
        <h2 id="background-title">Research, implementation, and teaching</h2>
        <p>{site.bio}</p>
        <p>At Oregon State, I led operating systems and computational methods studios and tutored more than 50 students in mathematics and computer science.</p>
        <ul class="link-row">
          <li><a class="button-link" href={resume.links.pdf}>Read my résumé <span class="file-type">PDF</span></a></li>
          {resume.links.variants.map((variant) => (
            <li><a href={variant.href}>{variant.label} <span class="file-type">PDF</span></a></li>
          ))}
          <li><a href={resume.links.linkedin}>LinkedIn</a></li>
          <li><a href="https://github.com/joses2017smjh">GitHub profile</a></li>
        </ul>
      </div>
      <div class="background-details">
        <h3>Education</h3>
        {resume.education.map((e) => <p><strong>{e.degree}</strong><br />{e.school} · {e.when}</p>)}
        <h3>Tools I use</h3>
        <p>Python, C++, PyTorch, FastAPI, Docker, PostgreSQL, Isaac Sim, MuJoCo, and Slurm.</p>
      </div>
    </section>

    <section class="beyond-teaser" id="beyond" aria-labelledby="beyond-title">
      <img src="/media/humanoid/26-fully-built-movement.jpg" alt="Fully assembled Berkeley Humanoid Lite hanging from a rack, chest screen on, wiring still external." width="1200" height="1600" loading="lazy" decoding="async" />
      <div>
        <p class="eyebrow">Away from the screen</p>
        <h2 id="beyond-title">Building the hardware, too.</h2>
        <p>The robot is fully built. First sign of life on 13 Sep. Outside the workshop, I hike and train jiu-jitsu.</p>
        <a href="/beyond">The build log and beyond <span aria-hidden="true">&rarr;</span></a>
      </div>
    </section>

    <section class="contact-block" id="contact" aria-labelledby="contact-title">
      <p class="eyebrow">Let's talk</p>
      <h2 id="contact-title">Hiring for robotics or perception?</h2>
      <p>I’m looking for a team building robot perception, simulation, or ML systems. My work pairs working demos with reproducible evaluations.</p>
      <a class="contact-address" href={`mailto:${site.email}`}>{site.email}</a>
    </section>
  </div>
</Base>
